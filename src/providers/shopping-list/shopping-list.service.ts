import { ProcessedShoppingList } from './shopping-list.types';

/**
 *
 * @param shoppingList string extract text and checkboxes
 * @returns the text to textarea and a list
 */
function format(shoppingList: string): ProcessedShoppingList {
  return {};
}
